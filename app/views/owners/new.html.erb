<% content_for :title do %>
  <%= render TitleComponent.new title: "Nouveau propriétaire", 
    subtitle: "Renseignez les informations suivantes :" %>
<% end %>

<% content_for :back_button, render(BackButtonComponent.new) %>

<%= render 'form', owner: @owner %>