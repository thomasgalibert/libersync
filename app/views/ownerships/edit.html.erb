<% content_for :title do %>
  <%= render TitleComponent.new title: "Modifier la propriété", subtitle: k(@lot).name %>
<% end %>

<% content_for :back_button, render(BackButtonComponent.new) %>

<%= render 'form', lot: @lot, ownership: @ownership %>