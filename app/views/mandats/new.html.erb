<% content_for :title do %>
  <%= render TitleComponent.new title: "Nouveau mandat", subtitle: "ASL : #{@hoa.name}" %>
<% end %>

<% content_for :back_button, render(BackButtonComponent.new) %>

<%= render 'form', mandat: @mandat, hoa: @hoa %>