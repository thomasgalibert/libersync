<% content_for :title do %>
  <%= render TitleComponent.new title: "Lot N° #{@lot.number} - #{@hoa.name}", subtitle: @ownership.owner.name %>
<% end %>

<% content_for :edit_button, render(EditButtonComponent.new(url: edit_lot_ownership_path(@lot, @ownership))) %>
<% content_for :delete_button, render(DeleteButtonComponent.new(url: lot_ownership_path(@lot, @ownership))) %>

<% content_for :back_button, render(BackButtonComponent.new) %>

<ul id="lots" class="mt-10 flex flex-col space-y-2 mb-10">
  <%= render partial: "/payments/payment", collection: @lot.payments.where(owner: @ownership.owner) %>
</ul>