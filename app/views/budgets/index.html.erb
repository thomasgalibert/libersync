<% content_for :title do %>
  <%= render TitleComponent.new title: "Mes budgets", subtitle: "Retrouvez ici la liste de vos budgets enregistrés." %>
<% end %>

<div class="mt-10"><%= render LinkComponent.new name: "Ajouter un budget", url: new_budget_path %></div>

<ul id="hoas" class="mt-10 flex flex-col space-y-2">
  <%= render @budgets %>
</ul>