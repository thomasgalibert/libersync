<% content_for :title do %>
  <%= render TitleComponent.new title: "Modifier le paiement", subtitle: "ASL : #{@hoa.name} | Budget #{@budget.year}" %>
<% end %>

<% content_for :back_button, render(BackButtonComponent.new) %>

<%= render 'form', payment: @payment, budget: @budget, hoa: @hoa %>