<% content_for :title do %>
  <%= render TitleComponent.new title: "Modifier l'association", subtitle: "Renseignez les informations suivantes :" %>
<% end %>

<% content_for :back_button, render(BackButtonComponent.new) %>

<%= render 'form', hoa: @hoa %>