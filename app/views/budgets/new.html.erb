<% content_for :title do %>
  <%= render TitleComponent.new title: "Nouveau budget", subtitle: "Renseignez les informations suivantes :" %>
<% end %>

<% content_for :back_button, render(BackButtonComponent.new) %>

<%= render 'form', budget: @budget %>